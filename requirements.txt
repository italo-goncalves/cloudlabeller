# Core
numpy>=1.24
scipy>=1.10
opencv-python>=4.8
pillow>=10          # polygon rasterisation (core/raster.py)

# UI / 3D
# Pin PySide6 < 6.8: the 6.8+ Qt Windows build links icuuc.dll (ICU), which
# clashes with the ICU shipped by Anaconda / Windows (WinError 127 on import).
# 6.7.3 is verified working in the Anaconda-based venv on this machine.
PySide6==6.7.3
pyvista>=0.43
pyvistaqt>=0.11

# Geometry / IO
trimesh>=4.0
laspy>=2.5
pyproj>=3.6          # CRS-aware export (ENU -> projected EPSG)

# Photogrammetry — code is written against the pycolmap 4.x API
# (cam_from_world as method, camera_mode kwarg, etc.)
pycolmap>=4,<5

# Label transfer — camera conversion verified against hylite 1.36's proj_persp.
# NOTE: hylite pulls in plyfile (GPLv3). The app never imports it (hylite only
# uses it lazily in its own PLY reader), and it is DELETED from binary
# distributions to keep them GPL-free — see tools/gen_third_party_licenses.py.
hylite>=1.36

# ML — Ítalo's U-Net is TensorFlow/Keras. Pin 2.19: 2.20+ wheels failed to
# load on this machine. NOTE: importing TF needs the MSVC-runtime preload in
# cloudlabeller/__init__.py (Anaconda ships a 2020 msvcp140.dll that otherwise
# shadows System32's and breaks the TF DLL init). Native-Windows TF is
# CPU-only since 2.11; GPU training would require WSL2.
tensorflow==2.19.*
scikit-image>=0.22   # augmentation (gamma correction) in ml/dataset_builder.py
